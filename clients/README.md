# ZMQ Clients (no ROS required)

Run on any PC to connect to the robot remotely:
- client_video.py    — view camera stream
- client_sensors.py  — view lidar/odom/battery data  
- client_cmd_keyboard.py — control with keyboard

Install dependencies:
```bash
pip install pyzmq opencv-python numpy pynput
```
